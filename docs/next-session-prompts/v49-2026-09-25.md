# Next-session prompt — **v49**, written 2026-09-25

**This is the file to paste.** `docs/NEXT-SESSION-PROMPT.md` is always current; superseded ones live in
`docs/next-session-prompts/`.

| version | written | produced by | what changed |
|---|---|---|---|
| **v49** | 2026-09-25 | Hamed asked for MiniMax-M3's performance improved as far as possible at unchanged quality | **0.20.0: MiniMax decode 2.89 → 3.51 tok/s on the same text with the same tokens (one GPU round trip per layer; decode borrows the idle prefill transient slots as cache), prefill 2-3x (chunk 8,192, lm_head on the last row only; 16k at 240 tok/s, same NLL). Budget, prediction, split reads and eviction policy measured and not shipped.** §18.1 |
| v48 | 2026-09-25 | Hamed downloaded MiniMax-M3 and asked for it on Cachalot, from the internal SSD | 0.19.0: MiniMax-M3 (3-bit MLX) runs through `./serve-minimax.sh` / `./chat-minimax.sh`: prefill 80-100 tok/s, decode 2.5-4.0 tok/s, tools, thinking and disk snapshots work. The internal GLM copy was deleted to make room (Hamed's choice): GLM now reads over USB.** §18 |
| v47 | 2026-09-25 | Hamed asked for the v44-v46 levers planned, built, measured and documented | **0.18.0: GLM prefill 60 to 90 tok/s, bit-identical (a store path that never evicts, plus the next layer read early), GLM's missing MLX buffer-cache cap added, GLM disk snapshots (a restart reuses the system block). DeepSeek: an `iostat` trace shows prefill is not read-bound on either drive; the lookahead A/B drowned in a 124-to-200 s drift across back-to-back prefills.** §17.1, §15.13 |
| v46 | 2026-09-25 | Hamed asked for GLM-5.3-Flash on Cachalot | 0.17.0: GLM through `serve-glm.sh` / `chat-glm.sh`. §17 |
| v45 | 2026-09-24 | same as v47, one day earlier | 0.16.0: disk snapshot store by last use; lookahead and local compression closed. §15.12 |

---

You are continuing work on **Cachalot**, an MLX runtime that runs DeepSeek V4.1 Flash (552B parameters, 40 layers,
384 routed experts per layer, top-6) and, since 0.17.0, GLM-5.3-Flash (42 MoE layers, 288 experts, top-8) on a single
96 GiB Mac Studio M3 Ultra by streaming routed experts from SSD. The user is Hamed; he runs the interactive model
himself in a separate terminal and expects terse replies in chat, complete prose in files.

**Read this first.** `docs/HANDOFF.md`'s "Start here (2026-09-25, 0.20.0)", "0.19.0" and "0.18.0" blocks, then
sections **18.1**, **18**, **17.1** and **15.13**, then 17 and 15.12.

**Hamed's standing priority order: Hermes usage first, vision second, speed/performance third.**

**Check the working tree before starting** (`git status --short`). If 0.20.0 is uncommitted, confirm with Hamed and
commit; the bump is already made. `docs/translations/` is Hermes's working output from the 2026-09-24 subagent
session, not ours: ask Hamed before deleting or ignoring it.

**The shipped configurations** (one runtime at a time; both servers use port 8011):

```bash
CACHALOT_MLX_WIRED_LIMIT_GIB=80 ./chat.sh --expert-budget-gib 52
```

```bash
cd /Users/hamedprooshani/Projects/deepseek-v41-mac && ./serve.sh
```

```bash
cd /Users/hamedprooshani/Projects/deepseek-v41-mac && ./chat-glm.sh
```

```bash
cd /Users/hamedprooshani/Projects/deepseek-v41-mac && ./serve-glm.sh
```

```bash
cd /Users/hamedprooshani/Projects/deepseek-v41-mac && ./chat-minimax.sh
```

```bash
cd /Users/hamedprooshani/Projects/deepseek-v41-mac && ./serve-minimax.sh
```

GLM reads from the X10Pro since 0.19.0 (its internal copy made room for MiniMax); ask Hamed before copying it back
(internal free space after MiniMax: ~160 GiB, GLM needs 169).

## Job 1 — the live read of 0.15.0-0.18.0 with Hermes. Needs Hamed.

Unchanged from v46 Job 1 for DeepSeek (`docs/manual-tests/hermes-desktop.md` §5, sampler beside it, a task that
dispatches `delegate_task` subagents; hosted `auxiliary.compression` first). New for GLM, if Hamed wants it: one
Hermes session on `./serve-glm.sh`. Read back: the first request's `[request]` line (prefill of the ~20k block,
expected ~4 min at ~90 tok/s), a restart, then startup printing `prefix snapshots: 1 loaded ...` and the first
request reusing the block; no swap in the sampler during a prefill.

## Job 2 — the Hermes subagent change. Needs Hamed's decision. Unchanged from v46 Job 2.

## Job 3 — the prefill drift. Autonomous, measured-first.

§15.13: six back-to-back 12k DeepSeek prefills slowed from 124-130 s to ~200 s, chunks from ~40 s to 60-80 s, on
either lookahead setting. Repeat the `iostat` trace (`benchmarks/iostat_ts.py disk0 disk6` beside the timeline,
which prints `EPOCH T0=`) on the *sixth* of six back-to-back runs, with the
drive's temperature beside it (`smartctl -a disk0` if installed; ask Hamed before installing anything). If the
internal drive's MB/s falls with temperature, the drift is thermal: price a cool-down or a read-rate cap. The GLM
runs showed no such drift over ~10 minutes (5.3 GiB/s steady), which is itself a clue (bigger reads, fewer IOPS).

## MiniMax-M3 jobs (§18, §18.1). Hamed's goal in the 0.20.0 session was MiniMax speed; ask whether it still is.

Where a token goes now (§18.1): ~196 ms of expert reads (45.6 misses x 23.6 MiB, at the internal SSD's wall) plus
~89 ms of everything else; the all-hit floor is 85 ms. Prefill is compute-bound at chunk 8,192 (~240 tok/s at 16k).

- **M5 — short follow-up prefills.** 24-31 new tokens took 4.7-9.3 s: below `SPECULATE_MIN_TOKENS` (128) the scan
  path reads ~60 experts per layer into transient slots and admits none, and the borrow's shrink evicts first.
  Try the LRU (admitting) path for small chunks; measure on an agent's tool-result turns (a dump replayed through
  `serve-minimax.sh`), prefill seconds plus the next reply's decode.
- **M1 — the long-context ceiling, then a Hermes session on `serve-minimax.sh`.** KV ~120 KB per token (wired 75.7
  GiB after a 17k request; ~7.7 GB of KV at 64k). §18.1's 60 GiB-budget run shows the collapse near the 80 GiB wired
  limit (0.36 tok/s). Grow one conversation to 32k and 64k with the sampler beside it and find where decode
  degrades. Levers: an 8-bit KV cache (a quality check first), a lower budget above some context, MSA's sparse
  attention (the conversion drops its indexer weights; check the original checkpoint).
- **M6 — the ~89 ms non-read part.** `GemmaRMSNorm` (four per layer) as `mx.fast.rms_norm` with a precomputed
  `1 + w` (not bit-identical: `NLL_LAST=1024` and `--compare` first), `mx.compile` of the attention's elementwise
  tail; `benchmarks/minimax_decode_floor.py` measures the floor and profiles it.
- **M4 — hotlist preload** for the first turn after a restart (32 % hits on a cold cache). Prediction is measured
  and off (§18.1: slower on a saturated drive); do not re-open it at 52 GiB on this drive.
- **M2 — quality gate.** Still no external reference: compare `NLL_LAST=1024` against the 4-bit or mixed-3_6bit
  conversion's NLL on the same text, or a hosted MiniMax-M3's log-probs if one exists.
- **M3 — tools with thinking off.** One test declined a tool call with thinking off; check a few prompts in both
  modes before recommending a default for Hermes (the template's `adaptive` mode is a third option).

## GLM jobs, reordered after 0.18.0 (§17, §17.1)

- **G2 — routing trace, hotlist preload, one-layer-early prediction.** Unchanged; decode is still 3.3-3.6 tok/s and
  purely miss-bound. Note §17.1's cold-start finding first: after a cold prefill the scan path leaves layers 3-16
  resident and decode is 5-10 % slower in two pairs than after the LRU path; a hotlist preload replaces both.
- **G4b — chunk 4,096 at a 48 GiB budget.** 137 tok/s at 8k but swaps at 16k with 52 GiB (§17.1). Price the
  budget's decode cost against the prefill gain with `glm_prefill_timeline.py` (`ROUNDS`, `DECODE_TOKENS`).
  Also: the per-expert `quantized_matmul` loop against one `gather_qmm` (the reference's) — compute is ~7.5 s of a
  20 s chunk and is now what the next layer's reads hide behind.
- **G3 — reply splice for GLM** (the template re-renders tool calls). **G5 — quality gate** (streamed vs stock
  SwitchGLU; `glm_prefill_timeline.py NLL_LAST=1024` is a start).

## Jobs 4-7 — the rest of the slow-window trigger and MLX buffer knobs; vision on Desktop-sized screenshots; prefill KL; 54 GiB collapse, Objective-C corpus. Unchanged from v46.

## What is closed, so nobody spends a session there

- **MiniMax expert budget above 52 GiB** (§18.1): 56 is slower (memory pressure doubles the non-read part), 60
  collapses to 0.36 tok/s, 64 runs out of Metal memory. More decode cache comes from the borrow, not the budget.
- **MiniMax one-layer-early prediction, split reads, SLRU/LFU** (§18.1): slower, no gain, under one miss per token.
- **MiniMax prefill chunk as a quality question** (§18.1): 4,096 vs 8,192 NLL 0.7940 vs 0.7945.

- **GLM prefill chunk size as a quality question** (§17.1): every pair of chunkings differs by mean KL ~0.024 per
  position with flat NLL; pick the chunk on speed and memory only.
- **GLM prefill at chunk 2,048** (§17.1): at the internal SSD's wall (4.8-5.3 GiB/s across the chunk).
- Everything v46 listed as closed still is. The next-chunk Engram lookahead stays off: §15.13 shows the drive it
  reads is idle, but no A/B arm wins outside the drift; reopen only after Job 3 removes the drift.

## Rules that still hold, and one new one

- **Decode A/Bs on MiniMax: same text for both arms, not fresh text.** Decode speed is dominated by the text's
  routing (one configuration ran 239-394 ms per token across four texts); the page cache carries nothing between processes
  under this load (`fast_reads` 0), so the same-text pair is the fair one. Prefill A/Bs still want fresh text on
  DeepSeek. And nothing else on the machine during an arm: a CPU-only replay beside one slowed its prefill 13 %. New.
- **Any new model path caps MLX's buffer cache** (`mx.set_cache_limit`). GLM ran without one until 0.18.0 and swapped
  as soon as the wired set grew (§17.1).
- **Replay a dump before touching a cache**, restarts included (`benchmarks/snapshot_store_replay.py`).
- **A prefill A/B needs fresh text per arm** (`FILLER_FILE`/`FILLER_OFFSET`); a 3.5 MB concatenation of the repo's
  `.md` and `.py` files gives room for dozens of arms (§17.1). And now: **alternate arms and look for drift**
  before reading means (§15.13).
- **`snapshot_store.NUMERICS_VERSION` / `glm.snapshots.GLM_NUMERICS_VERSION` move with numerics, not releases.**
- **A slow-window A/B needs a slow window**; separate processes, interleaved; never two runtimes at once.
- **MLX 0.32 arrays belong to the thread that built them until evaluated** (§15.6).
- **Hermes changes under you.** Diff the dumped system prompt (and `reasoning_effort`) before assuming a cache bug.
- `serve.sh` / `serve-glm.sh` guard with `pgrep -f "deepseek-v41/bin/python|cachalot\.cli"`: launch them from a
  command line that contains neither (a `pkill -f "cachalot.cli serve"` in the same shell command trips it).
- One change at a time, measured. Terse in chat, complete prose in files. Full copy-paste commands.

## The instruments

| tool | what it answers | cost |
|---|---|---|
| `./serve.sh` / `./serve-glm.sh` + `CACHALOT_SERVER_DUMP=path` | per-request `[request]` line; request bodies and reply token ids | — |
| `benchmarks/glm_prefill_timeline.py N` (+ `CACHALOT_MODEL_PATH` for MiniMax, `FILLER_FILE`, `FILLER_OFFSET`, `NLL_LAST`, `DECODE_TOKENS`, `ROUNDS`, `LOGITS_OUT`, `ROUTE_TRACE`, `DECODE_IDS_OUT`, `--compare`) | GLM / MiniMax per-chunk prefill seconds, store wait, GiB read, memory; NLL; decode ms/token split into store wait and the rest, misses, read ms per expert; route trace; decode ids for bit-identity | ~1-3 min |
| `benchmarks/minimax_decode_floor.py MODEL` | all-hit decode floor and a cProfile of it | ~1 min |
| `benchmarks/minimax_policy_replay.py TRACE` | cache policies (LRU, SLRU, LFU, Belady) at several budgets on a decode trace | ~1 min |
| `benchmarks/expert_read_speed.py MODEL --concurrent K` (fresh `SEED` per run) | expert read latency and GiB/s at a queue depth | ~30 s |
| `benchmarks/prefill_unwire_timeline.py N` (+ `FILLER_FILE`, `FILLER_OFFSET`) | DeepSeek per-chunk prefill, wired memory, keep-alives, lookahead hits, bit-identity; `EPOCH T0=` for `iostat` | ~2-4 min at 12k |
| `benchmarks/snapshot_store_replay.py DUMP [--then-subagents N]` | prefilled tokens with restarts under the disk store | ~15 s |
| `benchmarks/prefix_pin_replay.py DUMP [--gib G]` | prefilled tokens per request under the real `PrefixCache` | ~1 min |
| `benchmarks/slow_window_sampler.py` | memory, swap, GPU %, display, server reads every 10 s | — |
| `benchmarks/decode_anatomy.py` | expert wait against `rest` per token | ~45 s |
| `benchmarks/decode_fingerprint.py --decode-tokens 24` | bit-identity of a scheduling change | ~1 min per arm |
| `benchmarks/vision_ablation.sh ARM [--cases ...]` | verifiable image cases per ablation arm | ~1-3 min per arm |
| `docs/manual-tests/hermes-desktop.md` | Hamed's Desktop test script | ~1-2 h |
